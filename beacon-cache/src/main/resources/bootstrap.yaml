# 服务名称
spring:
  application:
    name: beacon-cache
  # 多环境
  profiles:
    active: dev
  # nacos注册中心地址
  cloud:
    nacos:
      discovery:
        server-addr: 47.120.25.129:8848
      # nacos配置中心地址:
      config:
        server-addr: 47.120.25.129:8848
        file-extension: yaml
        # beacon-cache-dev.yaml